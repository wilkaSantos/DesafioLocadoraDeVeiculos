import styled from 'styled-components';
import theme from '../../styles/theme';

export const Container = styled.input`
  width: 100%;
  padding: 12px;

  border: none;
  border-radius: 5px;
  background: ${theme.colors.gray_100};
  outline-color: ${theme.colors.blue_700};
  
`;